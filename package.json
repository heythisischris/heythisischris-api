{
  "name": "heythisischris",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc | true && cp package.json dist/package.json && cd dist && npm install --omit=dev && zip -r lambda.zip * > /dev/null && aws --profile heythisischris lambda update-function-code --function-name heythisischris --zip-file \"fileb://lambda.zip\" > /dev/null && rm -rf lambda.zip && cd .. && rm -rf dist"
  },
  "author": "",
  "license": "ISC",
  "imports": {
    "#src/*": "./*"
  },
  "type": "module",
  "dependencies": {
    "serverless-postgres": "^2.0.2"
  }
}