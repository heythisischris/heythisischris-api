{
  "name": "heythisischris",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild ./*.ts --bundle --platform=node --outdir=dist --external:@aws-sdk && cd dist && zip -r lambda.zip * > /dev/null && aws --profile heythisischris lambda update-function-code --function-name heythisischris --zip-file \"fileb://lambda.zip\" > /dev/null && rm -rf lambda.zip"
  },
  "author": "",
  "license": "ISC",
  "imports": {
    "#src/*": "./*"
  },
  "type": "module",
  "dependencies": {
    "@notionhq/client": "^2.2.13",
    "serverless-postgres": "^2.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.5"
  }
}
