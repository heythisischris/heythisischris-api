{
  "name": "heythisischris",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "yarn install && tsc | true && cp package.json dist/package.json && cp -r node_modules dist/node_modules && cd dist && zip -r lambda.zip * > /dev/null && aws --profile heythisischris lambda update-function-code --function-name heythisischris --zip-file \"fileb://lambda.zip\" > /dev/null && rm -rf lambda.zip && cd .. && rm -rf dist"
  },
  "author": "",
  "license": "ISC",
  "imports": {
    "#src/*": "./*"
  },
  "type": "module",
  "dependencies": {
    "serverless-postgres": "^2.1.0",
    "bedrock-node": "file:bedrock-node-v1.0.0.tgz"
  }
}